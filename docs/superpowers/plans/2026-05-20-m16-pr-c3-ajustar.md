# M16 PR-C3 — Ajustar (5 sub-abas) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Substituir `AgentStudio` (6 abas: config/instructions/learning/issues/runs/stats) pelo layout M16 §10 de 5 abas: **Identidade · Instruções · Habilidades · Comportamento · Histórico**. Reusa componentes existentes (ConfigTab, InstructionsTab, LearningPanel, IssuesTab, StatsTab, RunsTab) re-rotulando + agrupando. Cria 3 novos wrappers (IdentidadeTab, HabilidadesTab, HistoricoTab). AgentStudio.tsx deletado (orphan após swap).

**Architecture:** PR-C3 é semantic re-grouping, não refactor invasivo. Cada nova aba "M16" renderiza componente(s) existente(s):
- **Identidade** → novo `IdentidadeTab` (read-only: avatar + nome + role)
- **Instruções** → `InstructionsTab` (existing, direct reuse)
- **Habilidades** → novo `HabilidadesTab` (wraps `LearningPanel` — focus on skills)
- **Comportamento** → `ConfigTab` (existing — model, mode, permissions, etc.)
- **Histórico** → novo `HistoricoTab` (stacks IssuesTab + StatsTab + RunsTab com section headers)

Em `AgentAjustar.tsx`, substitui o render `<AgentStudio>` por novo TabBar + conditional render das 5 abas. Tipo `StudioTab` deletado. `AgentStudio.tsx` deletado (não tem outros consumidores — confirmado via grep).

**Tech Stack:** React 18 · TypeScript strict · react-i18next · Tailwind · existing TabBar primitive (`components/ui/TabBar.tsx` variant="underline").

**Spec:** `docs/superpowers/specs/2026-05-18-m16-interface-redesign-design.md` §10. Mockup: `docs/m16-mockups/screen-ajustar.html`. Base: HEAD `e9e5876` (M16 PR-C2 close).

---

## File map

**Criados (renderer, 3):**
- `apps/renderer/src/components/agent-panel/IdentidadeTab.tsx` — avatar (initials) + nome + role read-only display.
- `apps/renderer/src/components/agent-panel/HabilidadesTab.tsx` — wrapper around `LearningPanel`.
- `apps/renderer/src/components/agent-panel/HistoricoTab.tsx` — stacks IssuesTab + StatsTab + RunsTab com section headers.

**Modificados (renderer, 3):**
- `apps/renderer/src/routes/AgentAjustar.tsx` — substitui `<AgentStudio>` por novo TabBar de 5 abas + conditional render. Tipo local `AjustarTab` = `"identidade" | "instrucoes" | "habilidades" | "comportamento" | "historico"`.
- `apps/renderer/src/i18n/en-US.json` — adiciona `ajustar.*` block (5 tab labels + 3 historico section labels).
- `apps/renderer/src/i18n/pt-BR.json` — mirror.

**Deletados (renderer, 1):**
- `apps/renderer/src/components/agent-panel/AgentStudio.tsx` — orphan após Task 4 substituir o uso em AgentAjustar.

**Total:** 3 criados + 3 modificados + 1 deletado = 7 arquivos. ~5 tasks.

---

## Conventions

- Sempre `pnpm typecheck` antes de commitar.
- Pre-commit hook: prettier + eslint + gitleaks. Nunca `--no-verify`.
- Commits lowercase, ≤72 chars, sem `+`/`%`.
- Sem emojis — SVG inline.
- Identidade visual: Poppins, blue tokens.
- Renderer-only — zero mudança em main/shared.

### Comandos

Pasta raiz: `D:\Projetos pessoais\DashboardAgent`. PowerShell.

- Tests renderer um arquivo: `cd "D:\Projetos pessoais\DashboardAgent\apps\renderer"; npx vitest run <relative-path>`
- Tests full renderer: `pnpm --filter @prospero/renderer test`
- Typecheck workspace: `pnpm typecheck`
- Lint: `pnpm lint`

---

## Task 1: i18n keys — `ajustar.*` block

**Files:**
- Modify: `apps/renderer/src/i18n/en-US.json`
- Modify: `apps/renderer/src/i18n/pt-BR.json`

Context: 8 chaves novas — 5 labels das abas + 3 section headers do Histórico. Reusa `funcionario.title` (PR-C2) onde necessário.

- [ ] **Step 1: Add to `en-US.json`**

Open `apps/renderer/src/i18n/en-US.json`. Add a new top-level `"ajustar"` block (alphabetical — após `ajustes` se existir):

```json
  "ajustar": {
    "tabs": {
      "identidade": "Identity",
      "instrucoes": "Instructions",
      "habilidades": "Abilities",
      "comportamento": "Behavior",
      "historico": "History"
    },
    "historico": {
      "tarefas": "Tasks",
      "metricas": "Metrics",
      "execucoes": "Runs"
    }
  },
```

- [ ] **Step 2: Mirror in `pt-BR.json`**

```json
  "ajustar": {
    "tabs": {
      "identidade": "Identidade",
      "instrucoes": "Instruções",
      "habilidades": "Habilidades",
      "comportamento": "Comportamento",
      "historico": "Histórico"
    },
    "historico": {
      "tarefas": "Tarefas",
      "metricas": "Métricas",
      "execucoes": "Execuções"
    }
  },
```

- [ ] **Step 3: Parity test + typecheck**

```powershell
cd "D:\Projetos pessoais\DashboardAgent\apps\renderer"
npx vitest run src/i18n/parity.test.ts
```

Expected: pass.

```powershell
cd "D:\Projetos pessoais\DashboardAgent"
pnpm typecheck
pnpm --filter @prospero/renderer test
```

Expected: all green.

- [ ] **Step 4: Commit**

```powershell
git add apps/renderer/src/i18n/en-US.json apps/renderer/src/i18n/pt-BR.json
git commit -m "feat(m16): add ajustar tabs i18n keys"
```

---

## Task 2: `IdentidadeTab` component

**Files:**
- Create: `apps/renderer/src/components/agent-panel/IdentidadeTab.tsx`

Context: aba Identidade (M16 §10 row 1). Display simples (read-only por v1) de avatar + nome + role + status. Inicia via initials helper inline (mesmo padrão `TeamMemberRow.tsx` do PR-B1). Edit-in-place fica pra PR-G consolidation.

- [ ] **Step 1: Create the component**

Create `apps/renderer/src/components/agent-panel/IdentidadeTab.tsx` with EXACTLY:

```typescript
import type { FC } from "react";
import { useTranslation } from "react-i18next";
import type { Agent } from "@prospero/shared";
import { getAgentStatusInfo } from "../../lib/inicio/agent-status.js";

// M16 PR-C3 — Identidade tab. Display read-only de avatar + nome + role + status.
// Edit-in-place fica pra PR-G ou polish posterior.

type Props = {
  agent: Agent;
};

const initialsOf = (name: string): string => {
  const trimmed = name.trim();
  if (trimmed === "") return "?";
  const parts = trimmed.split(/\s+/);
  if (parts.length === 1) {
    return parts[0]!.slice(0, 2).toUpperCase();
  }
  return ((parts[0]?.[0] ?? "") + (parts[1]?.[0] ?? "")).toUpperCase();
};

export const IdentidadeTab: FC<Props> = ({ agent }) => {
  const { t } = useTranslation();
  const { label, dotColor } = getAgentStatusInfo(agent.status, t);
  return (
    <div className="p-8 max-w-xl">
      <div className="flex items-center gap-5">
        <span
          className="w-20 h-20 rounded-full flex items-center justify-center text-xl font-bold bg-brand-bg text-brand flex-shrink-0"
          aria-hidden="true"
        >
          {initialsOf(agent.name)}
        </span>
        <div className="min-w-0 flex-1">
          <h2 className="text-2xl font-bold text-ink">{agent.name}</h2>
          {agent.role !== "" && (
            <p className="text-base text-ink-muted mt-1">{agent.role}</p>
          )}
          <div className="flex items-center gap-2 mt-3 text-sm text-ink-soft">
            <span className={`w-2 h-2 rounded-full ${dotColor}`} aria-hidden="true" />
            <span>{label}</span>
          </div>
        </div>
      </div>
    </div>
  );
};
```

- [ ] **Step 2: Typecheck + lint**

```powershell
cd "D:\Projetos pessoais\DashboardAgent"
pnpm typecheck
pnpm lint
```

Expected: green.

- [ ] **Step 3: Commit**

```powershell
git add apps/renderer/src/components/agent-panel/IdentidadeTab.tsx
git commit -m "feat(m16): add identidade tab component"
```

---

## Task 3: `HabilidadesTab` component

**Files:**
- Create: `apps/renderer/src/components/agent-panel/HabilidadesTab.tsx`

Context: aba Habilidades (M16 §10 row 3). Wrapper around `LearningPanel` (existing, M11) — mostra skills + memories juntos. Não tenta separar skills de memories nesta versão (LearningPanel já mostra ambos com headers internos). PR-G pode polish se ficar muito confuso.

- [ ] **Step 1: Create the component**

Create `apps/renderer/src/components/agent-panel/HabilidadesTab.tsx` with EXACTLY:

```typescript
import type { FC } from "react";
import type { Skill, Memory } from "@prospero/shared";
import { LearningPanel } from "./LearningPanel.js";

// M16 PR-C3 — Habilidades tab. Wraps LearningPanel (existing M11 component
// that shows skills + memories). PR-G consolidation may polish split if needed.

type Props = {
  agentId: string;
  skills: Skill[];
  memories: Memory[];
};

export const HabilidadesTab: FC<Props> = ({ agentId, skills, memories }) => (
  <LearningPanel agentId={agentId} skills={skills} memories={memories} />
);
```

- [ ] **Step 2: Typecheck + lint**

```powershell
cd "D:\Projetos pessoais\DashboardAgent"
pnpm typecheck
pnpm lint
```

Expected: green.

- [ ] **Step 3: Commit**

```powershell
git add apps/renderer/src/components/agent-panel/HabilidadesTab.tsx
git commit -m "feat(m16): add habilidades tab component"
```

---

## Task 4: `HistoricoTab` component

**Files:**
- Create: `apps/renderer/src/components/agent-panel/HistoricoTab.tsx`

Context: aba Histórico (M16 §10 row 5) — combina Tarefas (IssuesTab) + Métricas (StatsTab) + Execuções (RunsTab) em sections empilhadas. Cada seção tem header pequeno (uppercase) + componente existente. Memories ficam em Habilidades (pra evitar duplicação).

- [ ] **Step 1: Create the component**

Create `apps/renderer/src/components/agent-panel/HistoricoTab.tsx` with EXACTLY:

```typescript
import type { FC } from "react";
import { useTranslation } from "react-i18next";
import { IssuesTab } from "./IssuesTab.js";
import { StatsTab } from "./StatsTab.js";
import { RunsTab } from "./RunsTab.js";

// M16 PR-C3 — Histórico tab. Stacks IssuesTab (tarefas) + StatsTab (métricas)
// + RunsTab (execuções) em sections empilhadas com section headers pequenos.

type Props = {
  agentId: string;
  companyId: string;
};

const SectionHeader: FC<{ label: string }> = ({ label }) => (
  <h3 className="px-6 pt-6 pb-2 text-[11px] font-bold uppercase tracking-wider text-ink-soft">
    {label}
  </h3>
);

export const HistoricoTab: FC<Props> = ({ agentId, companyId }) => {
  const { t } = useTranslation();
  return (
    <div className="flex flex-col">
      <SectionHeader label={t("ajustar.historico.tarefas")} />
      <IssuesTab agentId={agentId} companyId={companyId} />
      <SectionHeader label={t("ajustar.historico.metricas")} />
      <StatsTab agentId={agentId} />
      <SectionHeader label={t("ajustar.historico.execucoes")} />
      <RunsTab agentId={agentId} companyId={companyId} />
    </div>
  );
};
```

- [ ] **Step 2: Typecheck + lint**

```powershell
cd "D:\Projetos pessoais\DashboardAgent"
pnpm typecheck
pnpm lint
```

Expected: green.

- [ ] **Step 3: Commit**

```powershell
git add apps/renderer/src/components/agent-panel/HistoricoTab.tsx
git commit -m "feat(m16): add historico tab component"
```

---

## Task 5: Wire 5 abas in `AgentAjustar.tsx` + delete `AgentStudio.tsx`

**Files:**
- Modify: `apps/renderer/src/routes/AgentAjustar.tsx`
- Delete: `apps/renderer/src/components/agent-panel/AgentStudio.tsx`

Context: substitui `<AgentStudio>` por novo TabBar + conditional render das 5 abas. `AgentStudio.tsx` é orphan após esta mudança — deletar.

Tipo `AjustarTab` local. Aba default: "identidade".

`onOpenLearning` no AgentHeader vinha pra "learning" (aba antiga). Agora a aba é "habilidades" — mapear `onOpenLearning={() => setAjustarTab("habilidades")}`.

- [ ] **Step 1: Rewrite AgentAjustar.tsx**

Open `apps/renderer/src/routes/AgentAjustar.tsx`. Replace ALL contents with EXACTLY:

```typescript
import { useEffect, useState } from "react";
import { useParams } from "react-router-dom";
import { useTranslation } from "react-i18next";
import type { Skill, Memory } from "@prospero/shared";
import { useAgentsStore } from "../stores/agents.js";
import { AgentHeader } from "../components/agent-panel/AgentHeader.js";
import { BreadcrumbBar } from "../components/agent-panel/BreadcrumbBar.js";
import { IssueFormModal } from "../components/issues/IssueFormModal.js";
import { TabBar } from "../components/ui/index.js";
import { IdentidadeTab } from "../components/agent-panel/IdentidadeTab.js";
import { InstructionsTab } from "../components/agent-panel/InstructionsTab.js";
import { HabilidadesTab } from "../components/agent-panel/HabilidadesTab.js";
import { ConfigTab } from "../components/agent-panel/ConfigTab.js";
import { HistoricoTab } from "../components/agent-panel/HistoricoTab.js";

// M16 PR-C3 — /agents/:id/ajustar com 5 abas M16:
// Identidade · Instruções · Habilidades · Comportamento · Histórico.
// AgentStudio.tsx (M12 PR-F) foi deletado neste task.

type AjustarTab =
  | "identidade"
  | "instrucoes"
  | "habilidades"
  | "comportamento"
  | "historico";

const TABS: AjustarTab[] = [
  "identidade",
  "instrucoes",
  "habilidades",
  "comportamento",
  "historico",
];

export const AgentAjustar = () => {
  const { t } = useTranslation();
  const { id: agentId } = useParams<{ id: string }>();
  const agent = useAgentsStore((s) => s.agents.find((a) => a.id === agentId));
  const [skills, setSkills] = useState<Skill[]>([]);
  const [memories, setMemories] = useState<Memory[]>([]);
  const [ajustarTab, setAjustarTab] = useState<AjustarTab>("identidade");
  const [showAssignTask, setShowAssignTask] = useState(false);

  useEffect(() => {
    if (agent === undefined) return;
    void (async () => {
      const [s, m] = await Promise.all([
        window.prospero.learning.listSkills(agent.id),
        window.prospero.learning.listMemories(agent.id),
      ]);
      setSkills(s);
      setMemories(m);
    })();
  }, [agent, ajustarTab]);

  if (agent === undefined) {
    return <div className="p-8 text-ink-muted">{t("agent.notFound")}</div>;
  }

  return (
    <div className="flex flex-col h-screen min-w-0">
      <BreadcrumbBar agentId={agent.id} agentName={agent.name} mode="ajustar" />
      <AgentHeader
        agent={agent}
        onAssignTask={() => setShowAssignTask(true)}
        skillCount={skills.length}
        memoryCount={memories.length}
        onOpenLearning={() => setAjustarTab("habilidades")}
      />
      <div className="flex-1 flex flex-col min-h-0">
        <div className="px-6">
          <TabBar
            variant="underline"
            active={ajustarTab}
            onSelect={(id) => setAjustarTab(id as AjustarTab)}
            tabs={TABS.map((k) => ({ id: k, label: t(`ajustar.tabs.${k}`) }))}
          />
        </div>
        <div className="flex-1 min-h-0 overflow-y-auto">
          {ajustarTab === "identidade" && <IdentidadeTab agent={agent} />}
          {ajustarTab === "instrucoes" && <InstructionsTab agentId={agent.id} />}
          {ajustarTab === "habilidades" && (
            <HabilidadesTab agentId={agent.id} skills={skills} memories={memories} />
          )}
          {ajustarTab === "comportamento" && <ConfigTab agent={agent} />}
          {ajustarTab === "historico" && (
            <HistoricoTab agentId={agent.id} companyId={agent.companyId} />
          )}
        </div>
      </div>
      {showAssignTask && (
        <IssueFormModal
          companyId={agent.companyId}
          initialAssigneeId={agent.id}
          onClose={() => setShowAssignTask(false)}
        />
      )}
    </div>
  );
};
```

- [ ] **Step 2: Delete AgentStudio.tsx**

Confirm AgentStudio.tsx is no longer imported anywhere:

```powershell
cd "D:\Projetos pessoais\DashboardAgent"
git grep -n "AgentStudio" -- "*.ts" "*.tsx"
```

Expected: zero matches (only the file itself, if it still exists).

```powershell
git rm apps/renderer/src/components/agent-panel/AgentStudio.tsx
```

- [ ] **Step 3: Typecheck + lint + full suite**

```powershell
cd "D:\Projetos pessoais\DashboardAgent"
pnpm typecheck
pnpm lint
pnpm --filter @prospero/renderer test
pnpm --filter @prospero/main test
pnpm --filter @prospero/shared test
```

Expected: all green. Total tests unchanged.

- [ ] **Step 4: Pre-commit sanity**

```powershell
git status --short
git diff HEAD --stat
```

Confirm: `apps/renderer/src/routes/AgentAjustar.tsx` modified, `apps/renderer/src/components/agent-panel/AgentStudio.tsx` deleted.

- [ ] **Step 5: Commit**

```powershell
git add apps/renderer/src/routes/AgentAjustar.tsx apps/renderer/src/components/agent-panel/AgentStudio.tsx
git commit -m "feat(m16): replace agentstudio with 5-tab ajustar layout"
```

---

## Final verification

- [ ] **Step 1: Run the full suite**

```powershell
cd "D:\Projetos pessoais\DashboardAgent"
pnpm typecheck
pnpm lint
pnpm test
```

Expected: 4 packages green; total tests unchanged (1747 passing + 2 todo).

- [ ] **Step 2: Inspect commit graph**

```powershell
git log --oneline e9e5876..HEAD
```

Expected: 5 commits, all `feat(m16):` prefix.

- [ ] **Step 3: Smoke check (optional)**

If `pnpm dev` works:
- Sidebar "Minha equipe" → click an agent → `/agents/:id` (Conversa).
- Click "Ajustar" → `/agents/:id/ajustar` → 5 abas visíveis (Identidade default).
- "Identidade" → avatar grande + nome + role + status.
- "Instruções" → InstructionsTab (charter).
- "Habilidades" → LearningPanel (skills + memories).
- "Comportamento" → ConfigTab (model + role + mode + permissions).
- "Histórico" → 3 sections stacked (Tarefas / Métricas / Execuções).
- Learning badge no AgentHeader (se houver) → clicar abre "Habilidades" tab.

- [ ] **Step 4: Push + memory + handoff**

```powershell
git push origin main
```

Create `project_m16_pr_c3_lessons.md` + update MEMORY.md + session_handoff.md. PR-C inteiro (C1 + C2 + C3) está fechado nesta sessão.

---

## Notes for the implementer

- **Reusa componentes existentes** — ConfigTab, InstructionsTab, LearningPanel, IssuesTab, StatsTab, RunsTab ficam intocados. Wrappers novos (IdentidadeTab, HabilidadesTab, HistoricoTab) só adicionam estrutura.
- **ConfigTab em "Comportamento"** ainda mostra nome/role/etc (duplicação visual com Identidade). Aceitável v1; PR-G consolidation pode dividir ConfigTab.
- **Memories ficam em Habilidades** (não Histórico) — LearningPanel já mostra ambos. Evita duplicação cross-tab.
- **`onOpenLearning` agora aponta pra "habilidades"** — preserva o atalho do learning badge no AgentHeader.
- **Tipo `StudioTab` deletado** junto com AgentStudio.tsx. Nada mais referencia.
- **TabBar variant="underline"** — mesmo padrão usado em AgentStudio antigo.
- **`exactOptionalPropertyTypes`** pode reclamar em algum lugar — usar conditional spread se reclamar.
- **No emojis** — apenas SVG inline.
- **Sem testes em componentes/rotas** — convenção renderer.
- **PR-C inteiro fechado nesta sessão** — após PR-C3, atualizar memória notando que PR-C1 + PR-C2 + PR-C3 mergeados sequencialmente.
