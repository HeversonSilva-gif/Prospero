# M16 PR-B1 — Início (reskin Vitrine Matinal) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reskin `routes/Briefing.tsx` pro layout M16 do mockup `page-inicio.html`. 4 seções: Saudação (sem nome do usuário por default) · "Precisa de você" (usa `briefing.needsYou` do M14) · "O que aconteceu esta noite" (3 stat cards) · "Sua equipe agora" (avatars dos agentes ativos). Reusa todos os dados que o M14 PR-C já provê via `useBriefingStore` + agentes via `useAgentsStore`.

**Architecture:** Reskin in-place do `Briefing.tsx` — preserva `useBriefingStore` (load/markReviewed/subscribeInbox) + adiciona `useAgentsStore` pra "Sua equipe agora". Componentes auxiliares (StatCard, TeamMemberRow) extraídos pra `components/inicio/`. Sub-buckets "outros" do M14 (verified/failed/inProgress/learned) DESAPARECEM — drill via `/goals`, `/issues`, etc. Manchete `briefing.headline` (M14 cached) NÃO é exibida — a sub-linha da saudação ocupa o role.

**Tech Stack:** React 18 · TypeScript strict · react-i18next · Tailwind · zustand (existentes: `useBriefingStore` M14 + `useAgentsStore` M9) · inline SVG (line icons, sem icon library).

**Spec:** `docs/superpowers/specs/2026-05-18-m16-interface-redesign-design.md` §5. Mockup: `docs/m16-mockups/page-inicio.html`. Base: HEAD `1774fec` (M16 PR-A1 close).

**Reconciliação M14 ↔ M16:** spec §17 — o "Início" do M16 É a Vitrine Matinal. Mesma rota `/briefing`. Reusa data flow inteiro do M14; troca só a apresentação.

---

## File map

**Criados (renderer, 4):**
- `apps/renderer/src/components/inicio/StatCard.tsx` — card horizontal com ícone + número + label.
- `apps/renderer/src/components/inicio/TeamMemberRow.tsx` — avatar (initials) + nome + status com bolinha colorida.
- `apps/renderer/src/components/inicio/inicio-icons.tsx` — 4 SVG line icons (CheckCircle pra concluídas, Play pra em andamento, CreditCard pra gastos, AlertTriangle pra "Precisa de você" rows).
- `apps/renderer/src/lib/inicio/agent-status.ts` — helper puro `getAgentStatusLabel(status, t)` que mapeia AgentStatus → label PT/EN + cor da bolinha. + tests.

**Modificados (renderer, 3):**
- `apps/renderer/src/routes/Briefing.tsx` — reskin completo: nova estrutura JSX, novas sections, remove ItemRow inline / BucketHeader / SubBucket / othersExpanded state. Mantém `markReviewed` como link pequeno no header de "Precisa de você". Drop sub-buckets verified/failed/inProgress/learned + cost footer (substituídos pelos 3 stat cards).
- `apps/renderer/src/i18n/en-US.json` — bloco `inicio.*` com ~14 chaves novas + `agentStatus.*` com 5 chaves.
- `apps/renderer/src/i18n/pt-BR.json` — idem.

**Total:** 4 criados + 3 modificados = 7 arquivos.

---

## Conventions

- Sempre rodar `pnpm typecheck` antes de commitar (lesson [[project-m14-pr-a-lessons]]).
- Pre-commit hook: prettier + eslint + gitleaks. Nunca `--no-verify`.
- Commits lowercase, no `+`/`%`, ≤72 chars.
- **Sem emojis** — SVG inline ([[feedback-no-emojis]]).
- Identidade visual: Poppins, blue tokens, cantos arredondados.
- Renderer-only — zero mudança em main/shared.

### Comandos

Pasta raiz: `D:\Projetos pessoais\DashboardAgent`. PowerShell.

- Tests renderer um arquivo: `cd "D:\Projetos pessoais\DashboardAgent\apps\renderer"; npx vitest run <relative-path>`
- Tests full renderer: `pnpm --filter @prospero/renderer test`
- Typecheck workspace: `pnpm typecheck`
- Lint: `pnpm lint`

---

## Task 1: i18n keys — `inicio.*` + `agentStatus.*`

**Files:**
- Modify: `apps/renderer/src/i18n/en-US.json`
- Modify: `apps/renderer/src/i18n/pt-BR.json`

Context: adicionar 2 blocos novos no JSON aninhado. `inicio.*` cobre a tela Início. `agentStatus.*` cobre labels de status do agente reusados pelo TeamMemberRow + futuras telas (Minha equipe em PR-C). Parity test em `apps/renderer/src/i18n/parity.test.ts` valida simetria.

i18next plural support via `{{count}}` — usar separado `_one`/`_other` se precisar, mas pra PR-B1 vou usar uma chave única com `{{count}}` (sem plural forms — copy uniforme).

- [ ] **Step 1: Add to `en-US.json`**

Open `apps/renderer/src/i18n/en-US.json`. Add a new top-level `"inicio"` block (after the existing `"briefing"` block, alphabetical order is desirable but not enforced):

```json
  "inicio": {
    "greeting": "Hello!",
    "subline": "Your company worked while you slept. Here's the recap.",
    "noCompany": "Select a company to see your home.",
    "loading": "Loading…",
    "precisaDeVoce": "Needs you",
    "precisaDeVoceEmpty": "All clear. Nothing waiting on you right now.",
    "precisaDeVoceCount": "{{count}} waiting on your decision",
    "verButton": "See",
    "markReviewed": "Mark as reviewed",
    "oQueAconteceu": "What happened overnight",
    "tarefasConcluidas": "tasks done",
    "emAndamento": "in progress",
    "gastosHoje": "spent today",
    "suaEquipeAgora": "Your team right now",
    "suaEquipeAgoraEmpty": "You don't have any team members yet. Hire someone in \"My team\"."
  },
  "agentStatus": {
    "working": "working",
    "thinking": "thinking",
    "idle": "idle",
    "waiting": "waiting on you",
    "paused": "paused",
    "error": "error"
  },
```

(Add commas where appropriate. Place these blocks AFTER `briefing` and BEFORE alphabetically-later blocks.)

- [ ] **Step 2: Mirror in `pt-BR.json`**

Open `apps/renderer/src/i18n/pt-BR.json`. Add:

```json
  "inicio": {
    "greeting": "Olá!",
    "subline": "Sua empresa trabalhou enquanto você dormia. Aqui está o resumo.",
    "noCompany": "Selecione uma empresa para ver seu Início.",
    "loading": "Carregando…",
    "precisaDeVoce": "Precisa de você",
    "precisaDeVoceEmpty": "Tudo certo. Nada esperando você no momento.",
    "precisaDeVoceCount": "{{count}} esperando sua decisão",
    "verButton": "Ver",
    "markReviewed": "Marcar como visto",
    "oQueAconteceu": "O que aconteceu esta noite",
    "tarefasConcluidas": "tarefas concluídas",
    "emAndamento": "em andamento",
    "gastosHoje": "gastos hoje",
    "suaEquipeAgora": "Sua equipe agora",
    "suaEquipeAgoraEmpty": "Você ainda não tem funcionários. Contrate alguém em \"Minha equipe\"."
  },
  "agentStatus": {
    "working": "trabalhando",
    "thinking": "pensando",
    "idle": "parado",
    "waiting": "esperando você",
    "paused": "pausado",
    "error": "com erro"
  },
```

- [ ] **Step 3: Run parity test + typecheck**

```powershell
cd "D:\Projetos pessoais\DashboardAgent\apps\renderer"
npx vitest run src/i18n/parity.test.ts
```

Expected: pass.

```powershell
cd "D:\Projetos pessoais\DashboardAgent"
pnpm typecheck
pnpm --filter @prospero/renderer test
```

Expected: all green.

- [ ] **Step 4: Commit**

```powershell
git add apps/renderer/src/i18n/en-US.json apps/renderer/src/i18n/pt-BR.json
git commit -m "feat(m16): add inicio and agentStatus i18n keys"
```

---

## Task 2: `agent-status.ts` helper + tests

**Files:**
- Create: `apps/renderer/src/lib/inicio/agent-status.ts`
- Create: `apps/renderer/src/lib/inicio/agent-status.test.ts`

Context: helper puro que mapeia `AgentStatus` (from `@prospero/shared`) para `{ label, dotColor }` usados pelo TeamMemberRow. Testado por unit. Label vem de `t("agentStatus.<key>")` via injetada `t` (mesmo padrão `format-summary.ts` do M15 PR-A).

`AgentStatus = "idle" | "thinking" | "working" | "waiting" | "error" | "paused" | "terminated"` (from `apps/main/src/agents/repository.ts` and exported via `@prospero/shared`).

Mapping:
- `working` → "trabalhando" / green dot (`bg-semantic-success`)
- `thinking` → "pensando" / blue dot (`bg-brand`)
- `idle` → "parado" / gray dot (`bg-ink-soft`)
- `waiting` → "esperando você" / yellow dot (`bg-semantic-warning`)
- `paused` → "pausado" / yellow dot (`bg-semantic-warning`)
- `error` → "com erro" / red dot (`bg-semantic-danger`)
- `terminated` → não mostrado (filtrado antes de chegar aqui)

- [ ] **Step 1: Write failing test**

Create `apps/renderer/src/lib/inicio/agent-status.test.ts`:

```typescript
import { describe, it, expect } from "vitest";
import type { TFunction } from "i18next";
import { getAgentStatusInfo } from "./agent-status.js";

// Fake t() that returns the key.
const t = ((key: string) => key) as unknown as TFunction;

describe("getAgentStatusInfo", () => {
  it("working returns green dot and i18n key", () => {
    const info = getAgentStatusInfo("working", t);
    expect(info.label).toBe("agentStatus.working");
    expect(info.dotColor).toBe("bg-semantic-success");
  });

  it("thinking returns brand dot", () => {
    const info = getAgentStatusInfo("thinking", t);
    expect(info.dotColor).toBe("bg-brand");
  });

  it("idle returns ink-soft dot", () => {
    const info = getAgentStatusInfo("idle", t);
    expect(info.dotColor).toBe("bg-ink-soft");
  });

  it("waiting returns warning dot", () => {
    const info = getAgentStatusInfo("waiting", t);
    expect(info.label).toBe("agentStatus.waiting");
    expect(info.dotColor).toBe("bg-semantic-warning");
  });

  it("paused returns warning dot", () => {
    const info = getAgentStatusInfo("paused", t);
    expect(info.dotColor).toBe("bg-semantic-warning");
  });

  it("error returns danger dot", () => {
    const info = getAgentStatusInfo("error", t);
    expect(info.dotColor).toBe("bg-semantic-danger");
  });
});
```

- [ ] **Step 2: Run, see fail**

```powershell
cd "D:\Projetos pessoais\DashboardAgent\apps\renderer"
npx vitest run src/lib/inicio/agent-status.test.ts
```

Expected: FAIL — module not found.

- [ ] **Step 3: Implement**

Create `apps/renderer/src/lib/inicio/agent-status.ts`:

```typescript
import type { TFunction } from "i18next";
import type { AgentStatus } from "@prospero/shared";

// M16 PR-B1 — maps AgentStatus to {label, dotColor} for the
// "Sua equipe agora" section of Início (and reusable by Minha equipe in PR-C).
// `terminated` is intentionally absent — terminated agents are filtered out
// by the caller before reaching this function.

export type AgentStatusInfo = {
  label: string;
  dotColor: string;
};

const DOT_COLOR: Record<Exclude<AgentStatus, "terminated">, string> = {
  working: "bg-semantic-success",
  thinking: "bg-brand",
  idle: "bg-ink-soft",
  waiting: "bg-semantic-warning",
  paused: "bg-semantic-warning",
  error: "bg-semantic-danger",
};

const I18N_KEY: Record<Exclude<AgentStatus, "terminated">, string> = {
  working: "agentStatus.working",
  thinking: "agentStatus.thinking",
  idle: "agentStatus.idle",
  waiting: "agentStatus.waiting",
  paused: "agentStatus.paused",
  error: "agentStatus.error",
};

export const getAgentStatusInfo = (
  status: AgentStatus,
  t: TFunction,
): AgentStatusInfo => {
  if (status === "terminated") {
    return { label: "", dotColor: "bg-ink-soft" };
  }
  return {
    label: t(I18N_KEY[status]),
    dotColor: DOT_COLOR[status],
  };
};
```

- [ ] **Step 4: Run, confirm pass**

```powershell
cd "D:\Projetos pessoais\DashboardAgent\apps\renderer"
npx vitest run src/lib/inicio/agent-status.test.ts
```

Expected: 6 passing.

- [ ] **Step 5: Typecheck + commit**

```powershell
cd "D:\Projetos pessoais\DashboardAgent"
pnpm typecheck
git add apps/renderer/src/lib/inicio/agent-status.ts apps/renderer/src/lib/inicio/agent-status.test.ts
git commit -m "feat(m16): add agent status helper for inicio"
```

---

## Task 3: `inicio-icons.tsx` — 4 SVG icons

**Files:**
- Create: `apps/renderer/src/components/inicio/inicio-icons.tsx`

Context: 4 line icons inline para os stat cards e rows. ViewBox 24x24, stroke currentColor, padrão Lucide. Sem icon library.

Icons needed:
- `CheckCircleIcon` — para "tarefas concluídas" stat card (verde)
- `PlayIcon` — para "em andamento" stat card (gray, filled)
- `CreditCardIcon` — para "gastos hoje" stat card (gray)
- `AlertTriangleIcon` — para "Precisa de você" rows (yellow)

- [ ] **Step 1: Create the icons**

Create `apps/renderer/src/components/inicio/inicio-icons.tsx`:

```typescript
import type { FC } from "react";

// M16 PR-B1 — line icons inline para Início.
// 24x24 viewBox, stroke currentColor. Estilo Lucide line.

type IconProps = {
  className?: string;
  size?: number;
};

const STROKE_PROPS = {
  fill: "none",
  stroke: "currentColor",
  strokeWidth: 1.75,
  strokeLinecap: "round" as const,
  strokeLinejoin: "round" as const,
};

export const CheckCircleIcon: FC<IconProps> = ({ className, size = 18 }) => (
  <svg
    width={size}
    height={size}
    viewBox="0 0 24 24"
    {...STROKE_PROPS}
    aria-hidden="true"
    className={className}
  >
    <circle cx="12" cy="12" r="9" />
    <path d="M8 12.5 L10.7 15 L16 9.5" />
  </svg>
);

export const PlayIcon: FC<IconProps> = ({ className, size = 16 }) => (
  <svg
    width={size}
    height={size}
    viewBox="0 0 24 24"
    fill="currentColor"
    aria-hidden="true"
    className={className}
  >
    <path d="M8.5 5.5 V18.5 L19 12 Z" />
  </svg>
);

export const CreditCardIcon: FC<IconProps> = ({ className, size = 18 }) => (
  <svg
    width={size}
    height={size}
    viewBox="0 0 24 24"
    {...STROKE_PROPS}
    aria-hidden="true"
    className={className}
  >
    <rect x="3" y="6.5" width="18" height="12" rx="2.5" />
    <path d="M3 11 L21 11" />
  </svg>
);

export const AlertTriangleIcon: FC<IconProps> = ({ className, size = 18 }) => (
  <svg
    width={size}
    height={size}
    viewBox="0 0 24 24"
    {...STROKE_PROPS}
    aria-hidden="true"
    className={className}
  >
    <path d="M12 4 L21 19.5 H3 Z" />
    <line x1="12" y1="10" x2="12" y2="14" />
    <line x1="12" y1="16.5" x2="12" y2="16.7" />
  </svg>
);
```

- [ ] **Step 2: Typecheck + lint**

```powershell
cd "D:\Projetos pessoais\DashboardAgent"
pnpm typecheck
pnpm lint
```

Expected: green.

- [ ] **Step 3: Commit**

```powershell
git add apps/renderer/src/components/inicio/inicio-icons.tsx
git commit -m "feat(m16): add inicio svg line icons"
```

---

## Task 4: `StatCard` component

**Files:**
- Create: `apps/renderer/src/components/inicio/StatCard.tsx`

Context: card horizontal pra os 3 stat cards da seção "O que aconteceu esta noite". Layout: ícone à esquerda (colored background circle) + número grande + label embaixo. Props: icon (ReactNode), iconBg (tailwind class), iconColor (tailwind class), value (string ou number), label (string).

- [ ] **Step 1: Create the component**

Create `apps/renderer/src/components/inicio/StatCard.tsx`:

```typescript
import type { FC, ReactNode } from "react";

// M16 PR-B1 — stat card for the "O que aconteceu esta noite" section.
// Horizontal layout: rounded icon container + value (large) + label (small).

type Props = {
  icon: ReactNode;
  iconBg: string;
  iconColor: string;
  value: string;
  label: string;
};

export const StatCard: FC<Props> = ({ icon, iconBg, iconColor, value, label }) => (
  <div className="flex-1 flex items-center gap-3 bg-surface-card border border-surface-border rounded-lg px-4 py-3">
    <span
      className={`w-8 h-8 rounded-md flex items-center justify-center flex-shrink-0 ${iconBg} ${iconColor}`}
      aria-hidden="true"
    >
      {icon}
    </span>
    <div className="min-w-0">
      <div className="text-base font-bold text-ink leading-tight">{value}</div>
      <div className="text-xs text-ink-soft">{label}</div>
    </div>
  </div>
);
```

- [ ] **Step 2: Typecheck + lint**

```powershell
cd "D:\Projetos pessoais\DashboardAgent"
pnpm typecheck
pnpm lint
```

Expected: green.

- [ ] **Step 3: Commit**

```powershell
git add apps/renderer/src/components/inicio/StatCard.tsx
git commit -m "feat(m16): add inicio StatCard component"
```

---

## Task 5: `TeamMemberRow` component

**Files:**
- Create: `apps/renderer/src/components/inicio/TeamMemberRow.tsx`

Context: row pra cada agente ativo da seção "Sua equipe agora". Layout: avatar circular (initials) + nome + sub-line com bolinha colorida + status label. Reusa `getAgentStatusInfo` do helper criado em Task 2.

Initials: primeiras 2 letras do nome em uppercase. Se nome só tem 1 palavra (ex: "Ana") → "AN". Se múltiplas palavras → primeira letra das primeiras 2 (ex: "Ana Maria" → "AM"). Helper inline na função.

- [ ] **Step 1: Create the component**

Create `apps/renderer/src/components/inicio/TeamMemberRow.tsx`:

```typescript
import type { FC } from "react";
import { useTranslation } from "react-i18next";
import type { Agent } from "@prospero/shared";
import { getAgentStatusInfo } from "../../lib/inicio/agent-status.js";

// M16 PR-B1 — row for the "Sua equipe agora" section of Início.
// Avatar (initials, blue bg) + name + status (colored dot + label).

type Props = {
  agent: Agent;
};

const initialsOf = (name: string): string => {
  const trimmed = name.trim();
  if (trimmed === "") return "?";
  const parts = trimmed.split(/\s+/);
  if (parts.length === 1) {
    return parts[0]!.slice(0, 2).toUpperCase();
  }
  return ((parts[0]?.[0] ?? "") + (parts[1]?.[0] ?? "")).toUpperCase();
};

export const TeamMemberRow: FC<Props> = ({ agent }) => {
  const { t } = useTranslation();
  const { label, dotColor } = getAgentStatusInfo(agent.status, t);
  return (
    <div className="flex items-center gap-3 px-3 py-2">
      <span
        className="w-9 h-9 rounded-full flex items-center justify-center text-xs font-semibold bg-brand-bg text-brand flex-shrink-0"
        aria-hidden="true"
      >
        {initialsOf(agent.name)}
      </span>
      <div className="min-w-0 flex-1">
        <div className="text-sm font-medium text-ink truncate">{agent.name}</div>
        <div className="text-xs text-ink-soft flex items-center gap-1.5">
          <span className={`w-2 h-2 rounded-full ${dotColor} flex-shrink-0`} aria-hidden="true" />
          <span className="truncate">{label}</span>
        </div>
      </div>
    </div>
  );
};
```

- [ ] **Step 2: Typecheck + lint**

```powershell
cd "D:\Projetos pessoais\DashboardAgent"
pnpm typecheck
pnpm lint
```

Expected: green.

- [ ] **Step 3: Commit**

```powershell
git add apps/renderer/src/components/inicio/TeamMemberRow.tsx
git commit -m "feat(m16): add inicio TeamMemberRow component"
```

---

## Task 6: Reskin `routes/Briefing.tsx`

**Files:**
- Modify: `apps/renderer/src/routes/Briefing.tsx`

Context: substituir a estrutura JSX (header + Precisa de você dominante + Outros colapsável + cost footer) pela do mockup M16: Saudação + Precisa de você + O que aconteceu (3 stat cards) + Sua equipe agora (rows). PRESERVA todo o data flow do M14 (useBriefingStore load/markReviewed/subscribeInbox) + adiciona `useAgentsStore` pra equipe.

Sub-buckets verified/failed/inProgress/learned + cost footer + headline desaparecem. State `othersExpanded` removido.

- [ ] **Step 1: Rewrite Briefing.tsx**

Open `apps/renderer/src/routes/Briefing.tsx`. Replace the entire file contents with:

```typescript
import { useEffect, type FC } from "react";
import { Link } from "react-router-dom";
import { useTranslation } from "react-i18next";
import { useBriefingStore } from "../stores/briefing.js";
import { useCompaniesStore } from "../stores/companies.js";
import { useAgentsStore } from "../stores/agents.js";
import { formatCents } from "../lib/costs/formatCents.js";
import { StatCard } from "../components/inicio/StatCard.js";
import { TeamMemberRow } from "../components/inicio/TeamMemberRow.js";
import {
  CheckCircleIcon,
  PlayIcon,
  CreditCardIcon,
  AlertTriangleIcon,
} from "../components/inicio/inicio-icons.js";
import type { BriefingItem } from "@prospero/shared";

// M14 PR-C laid the data flow (useBriefingStore + subscribeInbox); M16 PR-B1
// reskins this route for the new "Início" layout per spec §5. The cached
// `briefing.headline` is no longer surfaced — the saudação subline replaces it.
// Sub-buckets (verified/failed/inProgress/learned) are gone; their detail is
// reachable via /goals, /issues, etc.

const NeedsYouRow: FC<{ item: BriefingItem; verLabel: string }> = ({ item, verLabel }) => (
  <div className="flex items-center gap-3 px-3 py-2.5 border-t border-surface-border first:border-t-0">
    <span
      className="w-8 h-8 rounded-md flex items-center justify-center flex-shrink-0 bg-semantic-warning-bg text-semantic-warning-fg"
      aria-hidden="true"
    >
      <AlertTriangleIcon size={17} />
    </span>
    <div className="flex-1 min-w-0">
      <p className="text-sm font-medium text-ink truncate">{item.label}</p>
      {item.detail !== "" && (
        <p className="text-xs text-ink-soft truncate">{item.detail}</p>
      )}
    </div>
    <Link
      to={item.route}
      className="text-xs font-semibold px-3 py-1.5 rounded-md bg-brand text-white whitespace-nowrap"
    >
      {verLabel}
    </Link>
  </div>
);

export const Briefing: FC = () => {
  const { t } = useTranslation();
  const activeCompanyId = useCompaniesStore((s) => s.activeId);
  const briefing = useBriefingStore((s) => s.briefing);
  const loading = useBriefingStore((s) => s.loading);
  const error = useBriefingStore((s) => s.error);
  const load = useBriefingStore((s) => s.load);
  const markReviewed = useBriefingStore((s) => s.markReviewed);
  const subscribeInbox = useBriefingStore((s) => s.subscribeInbox);
  const agents = useAgentsStore((s) =>
    s.agents.filter((a) => a.status !== "terminated"),
  );

  useEffect(() => {
    if (activeCompanyId === null) return;
    void load(activeCompanyId);
    const off = subscribeInbox(activeCompanyId);
    return off;
  }, [activeCompanyId, load, subscribeInbox]);

  if (activeCompanyId === null) {
    return (
      <div className="p-8">
        <p className="text-sm text-ink-muted">{t("inicio.noCompany")}</p>
      </div>
    );
  }

  if (loading && briefing === null) {
    return (
      <div className="p-8">
        <p className="text-sm text-ink-muted">{t("inicio.loading")}</p>
      </div>
    );
  }

  if (error !== null && briefing === null) {
    return (
      <div className="p-8">
        <p role="alert" className="text-sm text-semantic-danger">
          {error}
        </p>
      </div>
    );
  }

  if (briefing === null) return null;

  const needsYouCount = briefing.needsYou.length;

  return (
    <div className="p-8 max-w-3xl space-y-6">
      {/* Saudação */}
      <header>
        <h1 className="text-2xl font-bold text-ink">{t("inicio.greeting")}</h1>
        <p className="mt-1 text-sm text-ink-soft">{t("inicio.subline")}</p>
      </header>

      {/* Precisa de você */}
      <section>
        <div className="flex items-center justify-between mb-2">
          <h2 className="text-[11px] font-bold uppercase tracking-wider text-ink-soft">
            {t("inicio.precisaDeVoce")}
          </h2>
          {needsYouCount > 0 && (
            <button
              type="button"
              onClick={() => void markReviewed(activeCompanyId)}
              className="text-xs text-ink-muted hover:text-ink"
            >
              {t("inicio.markReviewed")}
            </button>
          )}
        </div>
        <div className="bg-surface-card border border-surface-border rounded-xl overflow-hidden">
          {needsYouCount === 0 ? (
            <p className="text-sm text-ink-muted italic px-4 py-6 text-center">
              {t("inicio.precisaDeVoceEmpty")}
            </p>
          ) : (
            <>
              <div className="flex items-center justify-between px-4 py-2.5 border-b border-surface-border">
                <p className="text-sm font-semibold text-ink">
                  {t("inicio.precisaDeVoceCount", { count: needsYouCount })}
                </p>
                <span className="text-[11px] font-bold bg-semantic-warning text-semantic-warning-fg rounded-full px-2 py-0.5">
                  {needsYouCount}
                </span>
              </div>
              {briefing.needsYou.map((item) => (
                <NeedsYouRow key={item.id} item={item} verLabel={t("inicio.verButton")} />
              ))}
            </>
          )}
        </div>
      </section>

      {/* O que aconteceu esta noite */}
      <section>
        <h2 className="text-[11px] font-bold uppercase tracking-wider text-ink-soft mb-2">
          {t("inicio.oQueAconteceu")}
        </h2>
        <div className="flex gap-3">
          <StatCard
            icon={<CheckCircleIcon size={17} />}
            iconBg="bg-semantic-success-bg"
            iconColor="text-semantic-success"
            value={String(briefing.verified.length)}
            label={t("inicio.tarefasConcluidas")}
          />
          <StatCard
            icon={<PlayIcon size={15} />}
            iconBg="bg-surface-soft"
            iconColor="text-ink-muted"
            value={String(briefing.inProgress.length)}
            label={t("inicio.emAndamento")}
          />
          <StatCard
            icon={<CreditCardIcon size={17} />}
            iconBg="bg-surface-soft"
            iconColor="text-ink-muted"
            value={formatCents(briefing.costCents)}
            label={t("inicio.gastosHoje")}
          />
        </div>
      </section>

      {/* Sua equipe agora */}
      <section>
        <h2 className="text-[11px] font-bold uppercase tracking-wider text-ink-soft mb-2">
          {t("inicio.suaEquipeAgora")}
        </h2>
        <div className="bg-surface-card border border-surface-border rounded-xl overflow-hidden">
          {agents.length === 0 ? (
            <p className="text-sm text-ink-muted italic px-4 py-6 text-center">
              {t("inicio.suaEquipeAgoraEmpty")}
            </p>
          ) : (
            agents.map((agent) => <TeamMemberRow key={agent.id} agent={agent} />)
          )}
        </div>
      </section>
    </div>
  );
};
```

Notes on the rewrite:
- The `useEffect` data-loading + `subscribeInbox` logic from M14 is preserved verbatim.
- `useAgentsStore` is the M9 store; it's already loaded on activeCompanyId change in App.tsx. No new `load(activeCompanyId)` call needed here.
- `BriefingItem` import is preserved (used by NeedsYouRow).
- `formatCents` import preserved.
- `othersExpanded` state and SubBucket/BucketHeader/ItemRow components are deleted (no longer needed).
- `t("briefing.*")` keys are replaced with `t("inicio.*")` (Task 1 keys).
- The existing `briefing.headline` field on the store is NOT read here — kept on the store for future use but not surfaced in PR-B1.
- Tailwind tokens used: `text-ink`, `text-ink-soft`, `text-ink-muted`, `bg-brand`, `bg-brand-bg`, `text-brand`, `bg-semantic-success`, `bg-semantic-success-bg`, `text-semantic-success`, `bg-semantic-warning`, `bg-semantic-warning-bg`, `text-semantic-warning-fg`, `text-semantic-danger`, `bg-surface`, `bg-surface-card`, `bg-surface-soft`, `border-surface-border`. All are existing tokens. If `text-semantic-warning-fg` doesn't exist (let me NOT assume — implementer should verify in `apps/renderer/tailwind.config.ts` or `apps/renderer/src/theme/tokens.css`), substitute with `text-ink` or the closest existing variant.

- [ ] **Step 2: Verify Tailwind tokens exist**

Run:

```powershell
cd "D:\Projetos pessoais\DashboardAgent\apps\renderer"
grep -E "semantic-warning-fg|semantic-success-bg|brand-bg" src/theme/tokens.css tailwind.config.ts 2>$null
```

If `semantic-warning-fg` doesn't exist, find the closest equivalent (likely `text-semantic-warning` for the icon stroke; the chip background `bg-semantic-warning` already provides the yellow, and the dark inner text can be `text-[#3a2e00]` inline or `text-ink-on-warning` if it exists). Adjust the JSX accordingly.

- [ ] **Step 3: Typecheck + lint + renderer tests**

```powershell
cd "D:\Projetos pessoais\DashboardAgent"
pnpm typecheck
pnpm lint
pnpm --filter @prospero/renderer test
```

Expected: all green. Tests delta: +6 from Task 2 (`agent-status.test.ts`).

- [ ] **Step 4: Smoke check (optional, if Electron available)**

```powershell
pnpm dev
```

If it runs:
- App opens; click "Início" in the sidebar.
- See "Olá!" + "Sua empresa trabalhou enquanto você dormia. Aqui está o resumo."
- See "Precisa de você" with count chip + rows OR empty state.
- See "O que aconteceu esta noite" with 3 stat cards (concluídas, em andamento, gastos).
- See "Sua equipe agora" with agent rows (initials avatar + name + colored status dot).
- Click an item in "Precisa de você" → navigates to detail route.
- Click "Marcar como visto" → cursor advances, list refreshes.

If `pnpm dev` cannot run, skip; tests + typecheck cover the regression net.

- [ ] **Step 5: Pre-commit sanity**

```powershell
git status --short
git diff HEAD --stat
```

Confirm only `apps/renderer/src/routes/Briefing.tsx` changed in this task. Net change: significantly negative (the old layout was ~200 lines; new layout is ~170 lines; net ~-30 lines).

- [ ] **Step 6: Commit**

```powershell
git add apps/renderer/src/routes/Briefing.tsx
git commit -m "feat(m16): reskin briefing as inicio per m16 layout"
```

---

## Final verification

- [ ] **Step 1: Run the full suite**

```powershell
cd "D:\Projetos pessoais\DashboardAgent"
pnpm typecheck
pnpm lint
pnpm test
```

Expected:
- typecheck: 4 packages green.
- lint: 4 packages green.
- tests: ~1747 total (1393 main + 100 shared + 204 renderer (was 198 + 6 from Task 2 agent-status) + 50 agent-runner).

- [ ] **Step 2: Inspect commit graph**

```powershell
git log --oneline 1774fec..HEAD
```

Expected: 6 commits (1 per task), all `feat(m16):` prefix.

- [ ] **Step 3: Memory + push**

Create `project_m16_pr_b1_lessons.md` capturing:
- HEAD SHA after merge.
- 6 commits, +6 tests, ~-30 LOC in Briefing.tsx.
- Decisions: saudação sem nome do usuário (defer to PR-F onboarding); sub-buckets M14 removidos (drill via outras telas); headline M14 não exibida; markReviewed preservado como link discreto.
- Any surprises (e.g., Tailwind tokens that needed substitution).

Update `MEMORY.md` index + `project_session_handoff.md` to mark PR-B1 done + identify next PR (B2 Ajustes).

Push:

```powershell
git push origin main
```

---

## Notes for the implementer

- **Reusa o data flow M14 inteiro** — `useBriefingStore` (load/markReviewed/subscribeInbox) é o mesmo. Não criar IPC novo. Não modificar a store.
- **agents filter** — só `status !== "terminated"`. Os terminados não devem aparecer no "Sua equipe agora".
- **agents order** — no order applied; use whatever order `useAgentsStore` returns. PR-G consolidation can sort by status priority if user requests.
- **Tailwind tokens** — `text-semantic-warning-fg` é a única chave que pode não existir; substituir por `text-ink` ou a variante semântica disponível. **Verifique antes de copiar verbatim.**
- **headline (M14 cached)** — fica na store mas NÃO é exibida no PR-B1. Não deletar do store; PR-G ou um futuro PR pode trazer de volta como kicker/hint.
- **`briefing.failed` + `briefing.learned`** — desaparecem da Início. Acessíveis via outras telas. Stats card "tarefas concluídas" usa `briefing.verified.length` (não soma com failed).
- **Initials helper** — inline em `TeamMemberRow.tsx`. Se cair em mais lugares depois, extrair pra `lib/inicio/initials.ts`.
- **No emojis** — apenas SVG inline. Mesma regra dos PRs anteriores.
- **Sem testes em componentes/rotas** — apenas `lib/inicio/agent-status.test.ts` é testado. Convenção renderer (lesson [[project-m11-pr-c-ui-lessons]]).
- **Reusa `useAgentsStore`** — já é carregado no efeito de bootstrap em App.tsx; não precisa de novo `load` aqui. Se em algum cenário edge `agents` estiver vazio porque o store ainda não carregou, a empty-state ("Você ainda não tem funcionários…") será mostrada momentaneamente. Aceitável.
